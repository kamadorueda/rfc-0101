diff --git a/discussions/whitespaces/after-nixfmt.nix b/discussions/whitespaces/after-nixpkgs-fmt.nix
index b52f611..abd9345 100644
--- a/discussions/whitespaces/after-nixfmt.nix
+++ b/discussions/whitespaces/after-nixpkgs-fmt.nix
@@ -18,4 +18,4 @@
   # Whitespace in function signatures
-  (a@{ }: _)
-  ({ }@a: _)
+  (a@{}: _)
+  ({}@a: _)
   ({ a }@b: _)
