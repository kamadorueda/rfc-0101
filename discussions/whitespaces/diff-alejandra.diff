diff --git a/discussions/whitespaces/before.nix b/discussions/whitespaces/after-alejandra.nix
index bc416ed..2554ee0 100644
--- a/discussions/whitespaces/before.nix
+++ b/discussions/whitespaces/after-alejandra.nix
@@ -11,6 +11,6 @@
   {}
-  {foo=bar;}
+  {foo = bar;}
   {
-    foo=bar;
-    bar=baz;
+    foo = bar;
+    bar = baz;
   }
@@ -18,6 +18,6 @@
   # Whitespace in function signatures
-  (a@{}: _)
-  ({}@a: _)
-  ({a}@b: _)
-  ({a}@b: _)
+  (a @ {}: _)
+  ({} @ a: _)
+  ({a} @ b: _)
+  ({a} @ b: _)
 ]
