diff --git a/discussions/string-interpolation/after-nixfmt.nix b/discussions/string-interpolation/after-alejandra.nix
index 5500100..3c3ebd7 100644
--- a/discussions/string-interpolation/after-nixfmt.nix
+++ b/discussions/string-interpolation/after-alejandra.nix
@@ -1,3 +1,7 @@
 ''
-  some-command --some-argument ${if cond then foo else bar}
+  some-command --some-argument ${
+    if cond
+    then foo
+    else bar
+  }
 ''
